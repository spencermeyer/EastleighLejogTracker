<p id="notice"><%= notice %></p>

<h1>Teams</h1>

<p>Runners may join teams to compete for the team event as well as the individual event.  There are a maximum of 4 runners per team.</p>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Full?</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @teams.each do |team| %>
      <tr>
        <td><%= team.name %></td>
        <!-- <td><%= team.text %></td> -->
        <td><%= team.full? ? 'full' : 'space left' %></td>
        <td><%= link_to 'Join', team_join_path(team) %></td>
        <td><%= link_to 'Show', team %></td>
<!--         <td><%= link_to 'Edit', edit_team_path(team) %></td> -->
<!--         <td><%= link_to 'Destroy', team, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Team', new_team_path %>